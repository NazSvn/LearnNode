// Entry arguments

// console.log(process.argv);

// control exit and entry process
// process.exit(0) //successful
// process.exit(1) //error

// process.on('exit', () => {
//do something, clean the recourses, clean console, etc});

// current working directory cwd tells from where the proccess is being executed
console.log(process.cwd());
